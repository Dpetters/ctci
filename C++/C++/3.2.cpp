#include "3.2.h"
#include <iostream>

using namespace std;

int run()
{
	/*
	StackWithMin myStack;
	int arr[] = { 6, 3, 5, 2, 2, 9, 2, 8, 1, 1 };
	for (int i = 0; i<10; i++)
	{
		myStack.push(arr[i]);
		cout << arr[i] << " " << myStack.min() << '\n';
	}
	for (int i = 0; i<10; i++)
	{
		myStack.pop();
		cout << i << " " << myStack.min() << '\n';
	}*/
	return 0;
}